name: animales_domesticos
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  # audioplayers: ^5.2.1 
  flutter:
    sdk: flutter
  audioplayers : ^6.4.0
  flutter_staggered_grid_view: ^0.7.0
  http: 1.4.0

  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  fonts:
      - family: ABeeZee-Regular
        fonts:
          - asset: assets/fonts/ABeeZee-Regular.ttf
      - family: GamjaFlower-Regular
        fonts:
          - asset: assets/fonts/GamjaFlower-Regular.ttf
  assets:
    - assets/images/
    - assets/sounds/
    - assets/data/
  uses-material-design: true
